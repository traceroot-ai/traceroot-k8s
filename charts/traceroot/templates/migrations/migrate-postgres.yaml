apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "traceroot.fullname" . }}-migrate-postgres
  labels:
    {{- include "traceroot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
    # Same reasoning as migrate-clickhouse: hook-succeeded destroys the migration
    # output at the moment it becomes the record of what a release did to the
    # database, and only before-hook-creation is needed to recreate the Job.
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: 3
  {{- include "traceroot.migrations.ttl" . | nindent 2 }}
  template:
    metadata:
      labels:
        {{- include "traceroot.selectorLabels" . | nindent 8 }}
        app: migrate-postgres
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      restartPolicy: Never
      containers:
        - name: migrate
          image: "{{ .Values.migrations.postgres.image.repository }}:{{ .Values.migrations.postgres.image.tag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.existingSecret }}
                  key: {{ .Values.postgresql.secretKeys.databaseUrl }}
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
